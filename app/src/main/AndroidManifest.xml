<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="android_demo"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".fragment.lifecycle.LifecycleActivity">
        </activity>

        <activity android:name=".fragment.addToBackStack.AddToBackStackActivity">
        </activity>

        <activity android:name=".fragment.communicate.activity_fragment.CommunicateMainActivity">


        </activity>


        <activity
            android:name=".fragment.singleInstance.SingleInstanceActivity"
            android:configChanges="orientation|keyboardHidden|screenSize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>
    </application>


</manifest>
